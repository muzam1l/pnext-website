{
  "name": "website",
  "private": true,
  "scripts": {
    "dev": "pnext dev",
    "build": "pnext build",
    "start": "pnext start",
    "analyze": "pnext analyze",
    "typecheck": "pnext typegen && tsc --noEmit"
  },
  "dependencies": {
    "marked": "^16.4.0",
    "preact": "^10"
  },
  "devDependencies": {
    "@wular/pnext": "^0.0.2"
  }
}
